<!DOCTYPE html>
<html>
  <head>
    <title>TomodachiApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class = "container">

      <% flash.each do |key, value| %>
        <%= content_tag(:div, value, class: "#{key}") %>
      <% end %>

      <header class = "header clearfix">
        <nav>
          <ul class = "top_menu clearfix"> 
            <li class = "right"><%= link_to "投稿一覧", feeds_path %>
            <li class = "right"><%= link_to '新規投稿', new_feed_path %>

          <% if logged_in? %>
            <li class = "right"><%= link_to "プロフィール", user_path(current_user.id) %>
            <li class = "right"><%= link_to "ログアウト", session_path(current_user.id), method: :delete %>
          <% else %>
            <li class = "right"><%= link_to "新規登録", new_user_path %>
            <li class = "right"><%= link_to "ログイン", new_session_path %>
          <% end %>
          </ul>
        </nav>
      </header>

      <div class="image_container">
        <%=image_tag "blog.webp", class: "image_area"%>
      </div>
      <%= yield %>
      <footer class="footer">
        <p>Copyright ©2023 starlog.</p>
      </footer>
    </div>
  </body>
</html>
